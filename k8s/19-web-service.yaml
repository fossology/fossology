apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: web
spec:
  ports:
    - name: "8081"
      port: 8081
      targetPort: 80
  selector:
    app: web
  type: LoadBalancer
status:
  loadBalancer: {}
