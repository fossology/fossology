#
# cron script for running schemaspy.  NOTE you must supply the db, 
# user and password. They are xxxxx for now.
#
java -jar /usr/local/bin/schemaSpy_3.1.1.jar -t pgsql \
-cp /usr/local/share/postgresql-8.0-320.jdbc3.jar \
-db xxxxx -s public -host localhost -u xxxx -p  -o /var/www/schemaSpy

